import 'package:flutter/material.dart';
void main()=>runApp(MiDicePage());
class MiDicePage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      home: Scaffold(

      ),
    );
  }
}